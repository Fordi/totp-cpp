# TOTP.h

Simple toy lib to:

1. Parse a TOTP URL
2. Generate codes from it

See [example/main.cpp](example/main.cpp) for an example of usage.

## Building

Run `make`.  With `-j$(nproc)` if you like, but there's really not much to compile there.